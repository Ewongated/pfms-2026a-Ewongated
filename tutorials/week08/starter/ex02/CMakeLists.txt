
##################################################################################################
# The below enables tests
# We have only provided 1 example
if(STUDENT_TESTS)
    add_subdirectory(student)
endif()
