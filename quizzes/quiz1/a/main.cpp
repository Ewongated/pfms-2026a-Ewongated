#include <iostream> // Includes std::cout and friends so we can output to console
#include "charger.h"
#include "processing.h"

int main (void) {

    //! Your main is not tested, we unit test classes you develop
    //! However, to assist your own development you can
    //! use the main to create objects, use functions of them
    //! and check the output

    //! You can also use the main to take user input and create objects
    //! and use functions to check the output

    //! Your classes that you develop (Charger and Processing) will be tested
    //! using the unit tests in the test folder
    //! These classes should NEVER take user input, and you should avoid output to console
    //! in these classes
    
  return 0;
}
